<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/arka_plan2"
    android:padding="16dp"
    tools:context=".ui.settings.NotificationSettingsFragment">

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="52dp"
        android:fontFamily="@font/barla"
        android:text="İzinler"
        android:textColor="@color/titleTextColor"
        android:textSize="72sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textViewInfo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="Uygulamanın doğru çalışması için aşağıdaki izinleri vermeniz gerekmektedir. İzinleri vermek için butonlara tıklayın."
        android:textColor="@color/titleTextColor"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textViewTitle" />

    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toTopOf="@+id/buttonContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textViewInfo">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonLocationPermission"
                style="@style/PermissionButton"
                android:text="Konum İzni Ver"
                app:icon="@drawable/ic_location_search"
                app:iconTint="@color/white" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonNotificationPermission"
                style="@style/PermissionButton"
                android:text="Bildirim İzni Ver"
                app:icon="@drawable/ic_notification"
                app:iconTint="@color/white" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonExactAlarm"
                style="@style/PermissionButton"
                android:text="Alarm Kurma İzni Ver"
                app:icon="@drawable/ic_notification"
                app:iconTint="@color/white" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonBatteryOptimization"
                style="@style/PermissionButton"
                android:text="Pil Optimizasyonunu Devre Dışı Bırak"
                app:icon="@drawable/ic_notification"
                app:iconTint="@color/white" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonAutostart"
                style="@style/PermissionButton"
                android:text="Otomatik Başlatma İzni Ver"
                app:icon="@drawable/ic_notification"
                app:iconTint="@color/white" />
        </LinearLayout>
    </ScrollView>

    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <TextView
            android:id="@+id/tvWarningMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:gravity="center"
            android:text="Gerekli izinleri vermezseniz uygulama düzgün çalışmayabilir. Namazı kaçırmanızdaki mesuliyet size aittir."
            android:textColor="@android:color/holo_red_light"
            android:textSize="12sp"
            android:visibility="gone" />

        <Button
            android:id="@+id/buttonSave"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="@drawable/custom_button"
            android:padding="12dp"
            android:text="Kaydet"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:visibility="visible" />   <!-- ✅ değiştirildi -->

        <Button
            android:id="@+id/buttonSkip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/custom_button"
            android:padding="12dp"
            android:text="İzinleri Vermeden Geç"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:visibility="visible" />   <!-- ✅ değiştirildi -->

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>